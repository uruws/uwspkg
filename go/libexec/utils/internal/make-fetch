#!/bin/sh
set -eu
pkgname=${1:?'pkg name?'}
fetch_cmd=${2:?'fetch command?'}
builddir=$(mktemp -p /build -d ${pkgname}-XXXXXXXX)
${fetch_cmd} BUILDDIR=${builddir}
basename ${builddir} >/build/.builddir
exit 0
