export UWSPKG_BUILD_SESSION="${UWSPKG_BUILD_SESSION}"
export UWSPKG_DEVELOPER_MODE="${UWSPKG_DEVELOPER_MODE}"
export UWSPKG_NAME="${UWSPKG_NAME}"
export UWSPKG_LOG="${UWSPKG_LOG}"
export UWSPKG_ORIGIN="${UWSPKG_ORIGIN}"
export UWSPKG_PROFILE="${UWSPKG_PROFILE}"
export UWSPKG_VERSION="${UWSPKG_VERSION}"
export UWSPKG_VERSION_NAME="${UWSPKG_VERSION_NAME}"
