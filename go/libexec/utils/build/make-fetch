#!/bin/sh
set -eu
sess=${1:?'session name?'}
origin=${2:?'pkg origin?'}
pkgname=${3:?'pkg name?'}
fetch_cmd=${4:?'fetch command?'}
srcdir=/uwspkg/src/${origin}
schroot_cmd="schroot -d ${srcdir} -c ${sess}"
${schroot_cmd} -- /uwspkg/libexec/internal/make-fetch ${pkgname} "${fetch_cmd}"
exit 0
