#!/bin/sh
set -eu
sess=${1:?'session name?'}
pkgname=${2:?'pkg full name?'}
make_cmd=${3:?'make cmd?'}
schroot_cmd="schroot -d /build -c ${sess}"
${schroot_cmd} -- /uwspkg/libexec/internal/make ${pkgname} "${make_cmd}"
exit 0
